--- a/internal/supervisor/supervisor.go
+++ b/internal/supervisor/supervisor.go
@@ -18,7 +18,6 @@
 
 const (
 	monitorInterval       = 2 * time.Second
-	unhealthyThreshold    = 3
 	initialRestartBackoff = time.Second
 	maxRestartBackoff     = 15 * time.Second
 	startupReadyLimit     = 12 * time.Second
@@ -30,12 +29,10 @@
 }
 
 type managedChild struct {
-	mu                     sync.Mutex
-	child                  Child
-	cmd                    *exec.Cmd
-	waitDone               <-chan error
-	adoptedPID             int
-	adoptedProcessInstance string
+	mu       sync.Mutex
+	child    Child
+	cmd      *exec.Cmd
+	waitDone <-chan error
 }
 
 func (m *managedChild) process() (*exec.Cmd, <-chan error) {
@@ -48,29 +45,9 @@
 	m.mu.Lock()
 	m.cmd = cmd
 	m.waitDone = waitDone
-	m.adoptedPID = 0
-	m.adoptedProcessInstance = ""
 	m.mu.Unlock()
 }
 
-func (m *managedChild) recordAdopted(health healthResponse) {
-	if health.PID <= 0 {
-		return
-	}
-	m.mu.Lock()
-	if m.cmd == nil {
-		m.adoptedPID = health.PID
-		m.adoptedProcessInstance = health.ProcessInstanceID
-	}
-	m.mu.Unlock()
-}
-
-func (m *managedChild) adoptedIdentity() (int, string) {
-	m.mu.Lock()
-	defer m.mu.Unlock()
-	return m.adoptedPID, m.adoptedProcessInstance
-}
-
 func (m *managedChild) clearProcess(cmd *exec.Cmd) {
 	m.mu.Lock()
 	if m.cmd == cmd {
@@ -161,7 +138,7 @@
 func (s *Supervisor) monitorChild(ctx context.Context, managed *managedChild) {
 	child := managed.child
 	backoff := initialRestartBackoff
-	unhealthy := 0
+	unhealthyLogged := false
 
 	for {
 		if ctx.Err() != nil || s.isStopping() {
@@ -190,17 +167,14 @@
 					continue
 				}
 				backoff = initialRestartBackoff
-				unhealthy = 0
+				unhealthyLogged = false
 				continue
 			default:
 			}
 		}
 
-		if health, ok := probe(child); ok {
-			if cmd == nil {
-				managed.recordAdopted(health)
-			}
-			unhealthy = 0
+		if ready(child) {
+			unhealthyLogged = false
 			backoff = initialRestartBackoff
 			if !sleepContext(ctx, monitorInterval) {
 				return
@@ -208,15 +182,11 @@
 			continue
 		}
 
-		unhealthy++
-		if cmd == nil || unhealthy >= unhealthyThreshold {
-			if cmd != nil {
-				// The process is alive but has stopped answering its bounded health
-				// surface. Reap only this owned child before attempting recovery.
-				log.Printf("supervisor: %s health failed %d consecutive checks; recycling owned child", child.Name, unhealthy)
-				_ = forceReapOwned(managed, cmd, done)
-				managed.clearProcess(cmd)
-			}
+		if cmd == nil {
+			// A connected/pre-existing service has disappeared (or was never
+			// available). Runtime may start its own SSPGPT child, but it does not
+			// acquire force-kill authority over an external process merely by
+			// observing a health endpoint.
 			if err := s.launchChild(ctx, managed); err != nil {
 				if ctx.Err() != nil || s.isStopping() {
 					return
@@ -228,11 +198,19 @@
 				backoff = minDuration(backoff*2, maxRestartBackoff)
 				continue
 			}
-			unhealthy = 0
 			backoff = initialRestartBackoff
+			unhealthyLogged = false
 			continue
 		}
 
+		// The owned process is still alive but its health surface is not ready.
+		// Degrade inward and observe; do not convert health misses into OS-level
+		// termination authority. If the process genuinely exits, the wait path
+		// above performs bounded restart recovery.
+		if !unhealthyLogged {
+			log.Printf("supervisor: %s health unavailable while process remains alive; leaving process ownership unchanged", child.Name)
+			unhealthyLogged = true
+		}
 		if !sleepContext(ctx, monitorInterval) {
 			return
 		}
@@ -244,8 +222,7 @@
 		return context.Canceled
 	}
 	child := managed.child
-	if health, ok := probe(child); ok {
-		managed.recordAdopted(health)
+	if ready(child) {
 		return nil
 	}
 	f, err := os.OpenFile(filepath.Join(s.logDir, child.Name+".log"), os.O_CREATE|os.O_APPEND|os.O_WRONLY, 0600)
@@ -276,7 +253,7 @@
 	ticker := time.NewTicker(150 * time.Millisecond)
 	defer ticker.Stop()
 	for {
-		if _, ok := probe(child); ok {
+		if ready(child) {
 			return nil
 		}
 		select {
@@ -363,10 +340,8 @@
 }
 
 type healthResponse struct {
-	Service           string `json:"service"`
-	Status            string `json:"status"`
-	PID               int    `json:"pid"`
-	ProcessInstanceID string `json:"process_instance_id"`
+	Service string `json:"service"`
+	Status  string `json:"status"`
 }
 
 func expectedService(child Child) string {
@@ -384,29 +359,21 @@
 	}
 }
 
-func probe(child Child) (healthResponse, bool) {
+func ready(child Child) bool {
 	cl := http.Client{Timeout: 350 * time.Millisecond}
 	resp, err := cl.Get(child.Health)
 	if err != nil {
-		return healthResponse{}, false
+		return false
 	}
 	defer resp.Body.Close()
 	if resp.StatusCode/100 != 2 {
-		return healthResponse{}, false
+		return false
 	}
 	var health healthResponse
 	if err := json.NewDecoder(resp.Body).Decode(&health); err != nil {
-		return healthResponse{}, false
-	}
-	if health.Status != "ready" || health.Service != expectedService(child) {
-		return healthResponse{}, false
+		return false
 	}
-	return health, true
-}
-
-func ready(child Child) bool {
-	_, ok := probe(child)
-	return ok
+	return health.Status == "ready" && health.Service == expectedService(child)
 }
 
 func shutdownRank(child Child) int {
@@ -473,42 +440,18 @@
 	deadline := time.NewTimer(shutdownLimit(child))
 	defer deadline.Stop()
 	if cmd == nil {
-		adoptedPID, adoptedInstance := managed.adoptedIdentity()
 		ticker := time.NewTicker(100 * time.Millisecond)
 		defer ticker.Stop()
 		for {
-			health, alive := probe(child)
-			if !alive {
+			if !ready(child) {
 				return nil
 			}
 			select {
 			case <-ticker.C:
 			case <-deadline.C:
-				// A connected/adopted child has no exec.Cmd owned by this Runtime.
-				// Force-reap it only when the final probe still proves the exact
-				// service process identity that Runtime adopted. This closes the
-				// no-orphan boundary without turning process-name enumeration into
-				// executable authority or risking an unrelated PID.
-				if adoptedPID > 0 && health.PID == adoptedPID &&
-					(adoptedInstance == "" || health.ProcessInstanceID == adoptedInstance) {
-					proc, findErr := os.FindProcess(adoptedPID)
-					if findErr == nil {
-						if killErr := proc.Kill(); killErr == nil {
-							for i := 0; i < 10; i++ {
-								if !ready(child) {
-									return fmt.Errorf("force-stopped adopted %s after shutdown deadline", child.Name)
-								}
-								time.Sleep(100 * time.Millisecond)
-							}
-						}
-					}
-				}
 				if requestErr != nil {
 					return fmt.Errorf("stop connected %s: %w", child.Name, requestErr)
 				}
-				if adoptedPID <= 0 {
-					return fmt.Errorf("connected %s remained alive after shutdown deadline and exposed no reaping identity", child.Name)
-				}
 				return fmt.Errorf("connected %s remained alive after shutdown deadline", child.Name)
 			}
 		}
