# CharacterGPT Update Channel

Network-update repository for CharacterGPT Prototype.

Current channel: `v05/`

Bootstrap components such as the Runtime executable, `dic00_system.txt`, `satori.dll`, Shell files, credentials, logs, and persistent profile data are intentionally excluded from normal SSP network updates.
